const components: any[] = [];

const install = (app: any): void => {
  components.forEach((comp: any) => {
    app.component(comp.name || comp.__name, comp);
  });
};

const plugin: any = { install };

export default plugin;
