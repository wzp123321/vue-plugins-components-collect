export interface ThemeProviderProps {
  mode?: 'light' | 'dark';
  customClass?: string;
  customStyle?: Record<string, string | number>;
}

export const defaultProps = {
  mode: undefined,
  customClass: '',
  customStyle: () => ({}),
} as const;
