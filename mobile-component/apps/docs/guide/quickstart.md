# 快速上手

## 使用组件

配置好 `easycom` 后，你可以直接在页面中使用组件，无需手动 import。

```vue
<template>
  <view>
    <up-button type="primary" text="确定"></up-button>
  </view>
</template>
```

## 注意事项

- 请确保你的开发环境安装了 HBuilderX 或对应的编译器。
- UniApp X 环境下，请确保使用的是兼容 UniApp X 的组件版本。
