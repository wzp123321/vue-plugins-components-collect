## 完成项

### 头部功能升级（国际化和主题切换）
- 安装 vue-i18n@9
- 创建 `src/locale/` 目录，包含 zh-CN.ts 和 en-US.ts 语言文件
- 创建 `useThemeStore` Pinia store 管理暗色/亮色主题
- 创建 `useLanguageStore` Pinia store 管理语言切换
- 创建 `HeaderActions.vue` 头部操作组件（主题切换 + 语言切换下拉）
- 更新 `container.vue` 集成头部操作区
- 创建 `dark-theme.less` 暗色主题样式覆盖文件
- 更新 `global.less` 添加暗色主题 CSS 变量
- 语言和主题设置会自动保存到 localStorage