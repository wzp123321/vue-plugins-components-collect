import { Add0ToHeadPipe } from './add-0-to-head.pipe';

describe('Add0ToHeadPipe', () => {
  it('create an instance', () => {
    const pipe = new Add0ToHeadPipe();
    expect(pipe).toBeTruthy();
  });
});
