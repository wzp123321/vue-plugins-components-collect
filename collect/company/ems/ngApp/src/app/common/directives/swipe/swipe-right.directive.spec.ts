import { SwipeRightDirective } from './swipe-right.directive';

describe('SwipeRightDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeRightDirective();
    expect(directive).toBeTruthy();
  });
});
