import { SwipeUpDirective } from './swipe-up.directive';

describe('SwipeUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeUpDirective();
    expect(directive).toBeTruthy();
  });
});
